
import {Layout} from "@/components";
import {useTranslations} from "next-intl";

const SearchPage = () => {
    return (
        <Layout>
            <div>
                SearchPage
            </div>
        </Layout>
    );
};

export default SearchPage;
